import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  components: {
    // Customize the theme for your components here
  },
  palette: {
    background: {
      default: '#FBF7FA', // Global background color
    },
  },
});

export default theme;
